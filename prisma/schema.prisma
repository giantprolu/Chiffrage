generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model User {
  id            Int            @id @default(autoincrement())
  username      String         @unique
  password      String
  createdAt     DateTime       @default(now())
  entries       Entry[]
  congeDays     CongeDay[]
  formationDays FormationDay[]
}

model Entry {
  id        Int      @id @default(autoincrement())
  date      DateTime
  client    String
  ticket    String?
  comment   String
  time      Float
  type      String?
  userId    Int?
  user      User?    @relation(fields: [userId], references: [id])
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model FormationDay {
  id     Int      @id @default(autoincrement())
  date   DateTime
  label  String   @default("FORMATION")
  userId Int?
  user   User?    @relation(fields: [userId], references: [id])

  @@unique([date, userId])
}

model CongeDay {
  id     Int      @id @default(autoincrement())
  date   DateTime
  label  String   @default("CONGÉ")
  time   Float    @default(1)
  userId Int?
  user   User?    @relation(fields: [userId], references: [id])
}
